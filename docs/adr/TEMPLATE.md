# ADR-NNNN: <タイトル(決定内容が一文で分かる形)>

このファイルは軽量ADR(Nygard/MADR minimal 折衷)のテンプレートである。アーキテクチャ判断を含む変更のとき、これを `docs/adr/NNNN-<slug>.md` にコピーして埋め、`docs/adr/INDEX.md` に1行追加する(`.claude/skills/adr/SKILL.md` が手順を補助する)。全変更に書く必要はない — 軽量+閾値が本テンプレートの方針。

- **日付**: YYYY-MM-DD
- **状態**: 提案 / 承認 / 廃止 / 置換(→ ADR-NNNN)

## 文脈

<なぜこの判断が必要になったか。制約・前提・問題。**上限20行** — 超えるなら判断を分割する。>

## 決定

<何をどうすると決めたか。能動態で簡潔に。>

## 検討した代替案(必須)

<最低1つ。各代替案に「なぜ選ばなかったか」を必ず添える。代替案の無い決定は判断ではなく作業記録。>

- **案A: <名前>** — <却下理由>
- **案B: <名前>** — <却下理由>

## 結果

<この決定で何が変わるか。良い影響・受け入れたトレードオフ・波及するファイルや運用。>

## Confirmation(実装後に追記)

<どう検証されたか。実行したコマンド・観測した事実・CI の結果。実装前は「未実装」と書き、実装後に必ず追記する(書きっぱなし防止)。>

## 信頼度と再検討トリガー

- **信頼度**: 高 / 中 / 低(根拠の強さと実証の有無)
- **再検討トリガー**: <この決定を見直すべき具体的条件(例: 管理リポジトリが5を超えたら・◯◯の制約が解消されたら)>
