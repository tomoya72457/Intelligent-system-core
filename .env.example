# このファイルは必要な環境変数の「名前と例」を共有する雛形。
# 実値は .env に書く(.env は .gitignore 済み・コミットしない)。
# 使い方: cp .env.example .env してから各値を設定する。

# 外部 API キー(ダミー値。実値へ置き換える)
EXAMPLE_API_KEY=replace-me-xxxxxxxxxxxxxxxx

# データベース接続文字列(例)
DATABASE_URL=postgres://user:password@localhost:5432/example
